{
  "name": "grantha-ocr-converter",
  "version": "1.0.0",
  "description": "An application to upload images, perform OCR, and convert the recognized text into Tamil Grantha script using the Gemini API.",
  "main": "server.js",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/ && esbuild index.tsx --bundle --platform=browser --outfile=public/bundle.js --external:react --external:react/* --external:react-dom/*",
    "start": "yarn build && node server.js"
  },
  "dependencies": {
    "@google/genai": "^1.12.0",
    "express": "^4.19.2",
    "esbuild": "^0.23.0"
  },
  "devDependencies": {}
}
